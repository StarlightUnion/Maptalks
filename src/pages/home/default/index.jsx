import React from "react";
import PageBuilder from "../../../common/pagerBuilder";
import "./index.less";

const HomePage = PageBuilder(() => {
  return (
    <main>{ "HomePage" }</main>
  );
});

export { HomePage };
